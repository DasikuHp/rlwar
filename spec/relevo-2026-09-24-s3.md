# Relevo 2026-09-24 — sesión 3 (auditoría de la sesión 2, cierre del paso 2, P2 empezado)

> No te fíes de este documento: compruébalo (git, batería, mutantes). La sesión se cortó porque el usuario tenía que
> irse: lo que no está hecho está dicho abajo.

## En una frase
Auditoría hecha y arreglada con tests primero (6 tests congelados en 076ae07; código en b9dc651, 8b69e9c y 281c60b);
**batería completa en verde sobre 281c60b: TODO OK, 89 grupos, 21 min 44 s**; subido. **P2 (mundos) empezado y en verde
en local pero SIN COMMITEAR** (falta congelar su test, mutantes y batería). Mutantes de la auditoría: tanda A casi entera,
con huecos por cubrir (abajo); tanda B (hilos) sin lanzar.

## Git
- Subido a origin/master: 076ae07 (tests congelados) · b9dc651 (motor) · 8b69e9c (percepción) · 281c60b (hilos, receta,
  cuerpos) · el commit de este relevo.
- **Sin commitear, en el disco (P2)**: `server/worlds.js` (nuevo), `test/mundos.spec.mjs` (nuevo, SIN congelar, pasa
  9/9 con el código), `spec/09-mundos.md` (nuevo), `evo/base/vidente-1.json` (nuevo, copia exacta de
  `evo/nets/vidente-1.json`), y cambios en `evo/store.js` (baseDir, setActiveDir, netsDir con evoDir), `evo/api.js`
  (labBusyReason, resetLab, pushLabEvent), `evo/busy.js` (anyHeld), `evo/train.js` (resetTrainerSeq) y `server/server.js`
  (ruta `/api/worlds`, `startWorlds()` al arrancar).
- ⚠️ Con ese código en el disco, arrancar el juego sin `GW_EVO_DIR` (Graphwar.bat) **mueve** `evo/nets`, `games`,
  `records`, `snapshots`, `throne.json` y `log*.jsonl` a `evo/archivo-2026-09-23/` (aprobado por el usuario; no borra).
- Si `git worktree list` muestra el worktree de mutantes del scratchpad: `git worktree prune`.

## Hallazgos de la auditoría y cómo quedaron
| # | gravedad | qué pasaba | estado |
|---|---|---|---|
| H1 | grave | la sala y los heurísticos pasaban el ángulo de ode2 en grados a un solver en radianes (30° → −81°); el Simulador sí convertía | ✅ b9dc651, `test/angulo` |
| H2 | alto | el servidor se bloqueaba hasta 1,4 s jugando partidas sin pantalla | ✅ 281c60b "hilos para todo" (`evo/threads.js`), `test/hilos`: duelo 1,36 s → 0,19 s; entreno de 1 hilo 1,16 → 0,20 s |
| H3 | medio | un cuerpo cortado a mitad se procesaba (creaba una sala) | ✅ 281c60b, `test/cuerpos` (+ tope exacto de `/api/lab`, que ya pasaba) |
| H4 | medio | `normalize()` copiaba el genoma dos veces por decisión (12 %) | ✅ 8b69e9c `observeNormalized` (7,2 %), guarda `test/normalize` |
| H5 | bajo | A3 (Adam por nº de parámetros) y parar en el examen de después | ✅ 281c60b, `test/receta-extra-b` |
| H6 | bajo | bocado guardado junto a la esquina de un rectángulo sin tocarlo | ✅ b9dc651, `test/terreno-extra-c` |
| H7 | bajo | el dibujo de la escena del editor solo pinta rectángulos y no bocados | ⏸ P6 |
| H8 | bajo | respaldo muerto en `percept.js:234-237` | ✅ quitado (8b69e9c) |
| H9 | bajo | detalles de colocación y tope de 256 bocados solo en tests | ✅ documentados (spec/01 §10.2, spec/08) |
| H10 | riesgo | margen de 1 s de A1 con carga | ⏸ **sin medir**: correr `receta-extra` 5 veces con carga |
| A1 | medio | reloj del entreno | ✅ 281c60b; `receta-extra` en verde |
Comprobado sin fallos (ejecutando): los cambios de 2f753b1 y 9fdbbf8 coinciden con su motivo (OK del usuario a 9fdbbf8);
atajos de `geometry.js` exactos (200 000 + 2 500 casos); Simulador idéntico al de antes de P1 con 0–1 impactos (8 973);
todo lo que simula recibe `bites`. Proceso: `mundos.spec` se ajustó una vez ANTES de congelarlo (esperaba `status` en vez
de `phase === 'stopped'`, que es la señal de la spec: un entreno parado aún guarda en su mundo unos 200 ms).

## Decisiones del usuario de esta sesión (faltan en plan2 las de P2)
OK a 9fdbbf8 · ángulo en sala y heurísticos · hilos para todo · los 4 arreglos menores. **P2**: Vidente 1 al repo
(`evo/base/vidente-1.json`) · migración al arrancar, una vez, solo en la carpeta real · al arrancar, el último mundo
abierto · borrar = papelera (`archivo-borrados/`).

## Mutantes (tanda A sobre 281c60b; los registros del scratchpad se pierden al apagar)
Cazados: lib 6/8 · policy 2/2 · solver 20/22 · reward 22/25 · geometry 152/182 · percept 193/229 · rooms 41/64 ·
server.js (cuerpos) 3/5 · api.js (cuerpos) 5/12 · mapgen ~155/209 (sin acabar) · train.js (A1/H5) sin acabar (7 vivos de 15).
**Equivalentes (justificar en spec/mutantes.md)**: caja del círculo (geo 16, `||`→`&&`, return null) · geo 32, 42, 60, 67,
70, 89 (return null) · solver 19, 57 · api.js 303, 304 y los códigos de 451 (nadie lee la respuesta de una conexión cerrada)
· server.js 50 (la ruta tampoco se ejecuta) · percept 221 (el primer punto va repetido), 224, 258 (empates), 259, 406
(campos del contexto que nadie lee) · rooms 68, 420 · mapgen 11 (cos(3πv) tiene la misma ley), 15, 16, 19, 24, 41 (`<=`),
42–43 (salvo con rng trucado).
**Huecos → test nuevo congelado (`test/huecos-s3.spec.mjs`, y con servidor `test/huecos-s3-api.spec.mjs`)**: lib.sim con
ode2 a 0° y con bocados · geometry: bordes exactos `dx = −R` y `dy = +R`, `slideMove` con 'stay'/null/undefined (reason y
slid), regla de empates (74), `los` (muestreo 0,25 u, extremo incluido, segmento corto, recta que cruza un círculo) ·
percept: caso inválido completo (entrada 8), topes de las entradas 5 y 8, "más cercano" con un solo enemigo, 2 500 pasos
(216), bocados en los destinos (253), adyacencia a 1 u (262), sin aliado / aliado a más de 10 u (264), orden de Obstáculos
(298), tope del Reloj (341), Compañeros con kills 0 y minDist 5 o ausente (360–362) · rooms: bocado con `y ≠ 0` y
tangencias exactas (391–392), turno y recorte a 200 de `shotLog` (403), `netId`/`agentType` en `game.start` (224),
mensaje "y a un aliado / N aliados" (436), banter con el nombre de la víctima y nunca del humano (438), `moveOptionsFor`
(324, cazado por moves.spec: comprobar) · reward: roce en `minDist = grazeRadius` y aliado a 1,5 u exactos · mapgen:
rangos completos (x de los grandes, llanura r 1–2,5, soldados x/y), altura de los grandes (−9 + 7i ± 1: escribirla en la
spec), separación 3 u, 400 intentos (rng trucado), rejilla de reserva (bordes y lado derecho) · cuerpos: cuerpo cortado en
`POST /api/lab/nets` (451) y en una sub-ruta de sala (server.js 117) · geometry 68 (`from` como candidato): preguntar
(la spec empieza la rejilla en r = 0,05; un soldado en el borde que pide salir se mueve 0,05 u).
**Sin lanzar**: tanda B (hilos: threads, worker, duel, children, exam, store, train.js 498-500, server.js 14-17, con
`trono,hilos` y `--timeout 240000`, 4 en paralelo como mucho) y repetir train.js; mutantes de P2.

## Qué falta, en orden
1. P2: revisar el diff del disco → congelar `mundos.spec` y añadirlo a run-all → commit del test → mutantes de
   `server/worlds.js` y de lo cambiado → batería → commit y push → plan2 (decisiones de P2) → lista "en qué fijarte".
2. Huecos de los mutantes (lista de arriba) → tests congelados → spec/mutantes.md (sección sesión 3) → commit.
3. Tanda B de mutantes; H10 (`receta-extra` 5 veces con carga).
4. P3 → P11 según el plan (`~/.claude/plans/pasted-content-id-4aad-trabajas-en-cozy-cake.md`).

# Relevo 2026-09-24 — sesión 5 (P1b cerrado en master, hueco train.js:501, carrera de api-verdad, "aprender en un hilo")

> No te fíes de este documento: compruébalo (git, batería, mutantes). Lo hecho en esta sesión se hizo con prisa en
> algunos puntos (los marcados **REVISAR**): la sesión siguiente empieza auditándolos.

## En una frase
P1b ya está en master y subido (batería TODO OK, 96 grupos). El hueco `train.js:501` se cierra con `hilos-b`. Una
carrera vieja de `api-verdad` queda arreglada (con OK). Se encontró que el servidor **deja de contestar mientras aprende**
y el usuario decidió **"aprender en un hilo", ahora, antes de P3**: spec en borrador y test `hilos-c` en borrador **sin
probar ni congelar**. P3 → P11 sin empezar.

## Git
- master = origin/master = `dc3dae9`: `be98069` (api-lab 11 → 12, api-verdad espera al duelo, hilos-b, tiempos por grupo
  en run-all, spec/mutantes.md) · `dc3dae9` (plan2 sesión 5 + borrador spec/11). La rama `p1b` = master (ya no hace
  falta; se puede borrar con OK del usuario).
- Este relevo va en el commit siguiente, junto con `test/hilos-c.spec.mjs` (BORRADOR: no está en run-all ni en FROZEN).
- `referencianoabsoluta.png` sin seguir en la raíz: es del usuario, no tocar.

## Hecho y comprobado (con la salida delante)
1. **Cierre de P1b**: `api-lab` 11 → 12 (OK del usuario; motivo `impossibleMove`). Batería completa: TODO OK, 96 grupos,
   27,4 min. `merge --ff-only` a master y push.
2. **`api-verdad` (carrera, OK del usuario "esperar al duelo")**: el caso del diario reta al trono con la red; si ya hay
   reina (en la batería la deja `api-trono`) arranca un duelo que tiene ocupada a la red, y la bofetada de después queda
   en cola (lo correcto, spec/04 §10.5) en vez de aplicarse. Reproducido: con `api-trono` antes falla 4 de 5 veces en p1b
   y 1 de 2 en master; solo, pasa. Arreglo: si el reto devuelve 202, el test espera a que acabe el duelo. 3/3 en verde +
   el caso sin reina.
3. **`hilos-b` (hueco train.js:501)**: el mismo entreno turbo de 1 hilo (redes lentas, 3 soldados, 8 partidas, semilla
   17, lote de 1) jugado en el hilo principal del test lo bloquea > 1 000 ms (premisa medida: 2,2–2,6 s); por la API el
   servidor contesta **todas** las peticiones (estado y salud) en < 500 ms desde la partida 1 (medido 72–107 ms).
   Mutantes train.js:499–501: 6/8 (501 ×2 cazados; 500 `===`→`!==` y `1`→`2` equivalentes con 1 hilo; los otros 4 de la
   500 los caza la premisa). **REVISAR**: la premisa usa el propio `train.js`; se mide desde la partida 1 porque la
   partida 0 (se guarda entera como muestra) y la primera actualización en frío bloquean 0,3–0,8 s (eso lo arregla spec/11).
4. **Tiempo de la batería (pendiente 4)**: en limpio 22,9 min; con `hilos-b` 27,4 min; con mis sondeos a la vez, 28,7.
   Entre las dos limpias la diferencia está repartida (+20–36 s en muchos grupos: `arreglos-ocupada` 216 → 252 s,
   `hilos` 49 → 80 s…): apunta a la carga de la máquina, no a un test. Los 34 min de la sesión 4 no se reproducen; la
   causa (carga, o las 12 126 carpetas de TEMP) **no está aislada**. `run-all` ya imprime el tiempo de cada grupo.
5. **Mutantes train.js 103 / 293 / 575 contra `receta`, `receta-entreno`, `receta-api`, `aprendizaje`,
   `aprendizaje-extra`, `entrenador-extra`, `entrenador-extra-b`** (parcial, se paró al cerrar la sesión):
   - 575 `&&`→`||`: **cazado** (receta-entreno). 575 `===`→`!==`: sobrevive aquí; según la tanda anterior lo cazaba
     `receta-extra-b` (**REVISAR**: no lo he vuelto a comprobar).
   - 103 `return null`: cazado. 103 `t: 0 → 1` (primer paso de Adam con otra corrección de sesgo) y `mean: 0 → 1` (la base
     "media" empieza en 1): **HUECOS REALES** → test con oráculo independiente: primer paso de Adam de libro
     (Kingma & Ba: Δθ = −lr·g/(|g|+ε)) y primera ventaja = G − 0. Ojo: spec/04 no dice que la media empiece en 0; dejarlo
     escrito (pregunta al usuario si hace falta). 103 `n: 0 → 1`: **equivalente** (`meanState.n` solo se suma, nunca se lee).
   - 293 `&&`→`||` ×2 y `===`→`!==`: sobreviven a esta lista; probablemente los caza `receta-extra-b` (A3, "Adam por
     estructura"). Los mutantes 8–12 de la 293 (`o.t || 0`, `o.mean || …`) **no llegaron a correr**. Repetir:
     `node tools/mutants.mjs evo/train.js --tests test/receta.spec.mjs,test/receta-entreno.spec.mjs,test/receta-api.spec.mjs,test/receta-extra.spec.mjs,test/receta-extra-b.spec.mjs,test/aprendizaje.spec.mjs,test/aprendizaje-extra.spec.mjs,test/entrenador-extra.spec.mjs,test/entrenador-extra-b.spec.mjs --lines 103,293,575 --timeout 240000`
     (en primer plano o con `run_in_background` del Bash, **no** con `*>` de PowerShell en segundo plano: salió 255 sin
     probar nada).
6. **Hallazgo: el servidor aprende en su hilo principal** (sondeos medidos, con el código de P1b): sueño de 4 partidas con
   redes lentas ≈ 0,85 s sin contestar; Vidente normal 0,15–0,45 s; la primera actualización de un entreno 0,3–0,8 s (en
   frío + `packGame` de la partida de muestra 0 = JSON + gzip); crece con el lote (hasta 64). `hilos.spec` no lo ve porque
   solo cronometra `/api/health`. **Decisión del usuario**: "Aprender en un hilo", **ahora, antes de P3**; `hilos.spec` se
   queda como está. En plan2 (sesión 5).

## Pendiente, en orden
0. **Auditar esta sesión** (lo marcado REVISAR) y comprobar: `git status`, `git worktree prune`,
   `node test/run-all.mjs --check-only` (96 congelados), batería en segundo plano.
1. **Aprender en un hilo (spec/11, borrador)**:
   a. Medir las premisas antes de fijar números: en el proceso, "de la última partida del lote al evento `sleep`" con
      redes lentas, 3 soldados, lote 4 (sospecha: ~0,85–1,2 s, justo; quizá haga falta lote 8 y 16 partidas); y el duelo
      `mix` (aprender tras cada partida y repaso final). Sondeos listos para copiar: ver "Sondeos" abajo.
   b. Ajustar `test/hilos-c.spec.mjs` (borrador, **nunca ejecutado**) y spec/11 §3 a lo medido; ejecutarlo contra el
      código de hoy: debe salir **en rojo por la latencia** y en verde en las premisas y en lo bit a bit (que hoy se cumple
      trivialmente: todo aprende en el hilo principal). Congelar y meterlo en run-all.
   c. Código: mensaje `learn` en `evo/worker.js`; `sleep` del entreno asíncrono (todas sus llamadas con `await`), con
      `learnFromGames` + empaquetado de las muestras en el hilo y el hilo principal aplicando pesos, Adam e Imaginación; el
      aprendiz de los duelos igual (`learn`/`review`), sin romper los aprendices inyectables (`opts.learner`). Mismo orden
      (el lote siguiente espera al sueño). Sin grupo, todo como hoy.
   d. Mutantes de train.js (sueño), duel.js, worker.js; batería; commit y push; relevo y memoria; lista "en qué fijarte".
2. Tests para los huecos 103 (`t`, `mean`) y lo que quede de 293 tras repetir los mutantes (punto 5 de arriba).
3. Huecos pequeños de la sesión 4: `resetTrainerSeq` sin test propio (P2); recorte ±3 del ajuste fino (policy 134); rama
   del soldado caído (rooms 520/522).
4. Carpetas `gw-*` en TEMP: los tests no las borran (había 953 a media sesión). Preguntar si se limpian y si los tests
   deberían borrar las suyas.
5. **P3 → P11** en el orden del plan (el usuario no quiere adelantar P5). P3 = "lo que aprende de este tiro" (datos): ya
   existen `shared/reward.js` (desglose por término, con `impossibleMove`), `decision.value` (con `hand.value`) y
   `POST /nets/:id/whatif`. Falta la escena exacta de cada decisión y la ventaja; en un duelo la red aprende al acabar
   cada partida (`hot`/`mix`), no tras cada tiro: el "antes/después del sueño" es por partida. Hay decisiones de diseño:
   preguntar.

## Sondeos (en el scratchpad de la sesión 5; se pierden: rehacerlos si hace falta)
- `sondeo-hilos.mjs <fast|slow> <soldados> <partidas> <semillas…>` (variables `LEARN`, `ALUMNA`, `SONDEO_ROOT`): entreno
  turbo de 1 hilo en el proceso; imprime los bloqueos > 100 ms del hilo principal y los eventos `curve`/`sleep` con su ms.
- `sondeo-duelo.mjs <learning> <soldados> <semillas…>`: duelo turbo en el proceso con `play` envuelto; imprime lo que
  tarda aprender tras cada partida y el repaso final. **Nunca ejecutado.**
- `repro-verdad.mjs [tests previos…]`: servidor nuevo, corre esos tests y después api-verdad.
- Truco para aislar "solo el hilo principal sin partidas": un worktree de pruebas con `train.js:500` `>`→`>=` (grupo
  propio de 1 hilo) hace que las partidas vayan a un hilo y en el hilo principal quede solo apuntar y aprender.

## Problemas de esta sesión (y cómo quedaron)
- La primera batería corrió a la vez que mis sondeos (28,7 min, tiempos contaminados): la segunda, en limpio.
- `hilos-b` salió mal dos veces antes de estar bien: medía solo `/api/health` (sesgado) y luego fallaba por el guardado de
  la partida 0 y el aprendizaje en frío. Rehecho midiendo todo y desde la partida 1.
- `git commit -F -` con un here-string de PowerShell no pasa el mensaje (git lo toma como rutas): nada se commiteó; se
  hizo con ficheros de mensaje. `Get-Content -Raw | Set-Content` de PowerShell 5.1 estropea el UTF-8: usar Edit,
  `[IO.File]` o node. `node -e` con comillas dentro de PowerShell se rompe: script en fichero.
- Mutantes en segundo plano con `*>` de PowerShell: salida 255 sin probar nada.
- Mutantes de 103/293 parados al cerrar (ver punto 5).

## Decisiones del usuario de esta sesión
OK a api-lab (11 → 12) · OK a api-verdad "esperar al duelo" · "Aprender en un hilo", ahora, antes de P3 · `hilos.spec`
se queda como está.

## "En qué fijarte" de P1b (ya en master; se ve en cualquier partida del laboratorio)
1. En el registro de una partida con redes, líneas `🚫 <nombre> pidió un sitio imposible (<motivo>) y pierde el
   movimiento`: ese turno el soldado no se mueve. Fallo: que se mueva un poco ("deslizado") o que falte el motivo.
2. Los movimientos normales son más cortos (1,5 u): los soldados avanzan menos por turno que antes.
3. En el editor, bloque Recompensa: perilla nueva "Movimiento imposible" en −0,3. Fallo: que no esté o que valga 0.
4. En Moverse → "Afinar destino", la explicación ya no dice "se vuelve a deslizar".
5. Los heurísticos (Greedy, Artillero, Caos, Francotirador) nunca muestran la línea 🚫.

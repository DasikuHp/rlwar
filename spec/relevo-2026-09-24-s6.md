# Relevo 2026-09-24 — sesión 6 (auditoría s4/s5, aprender en un hilo, duelo mix y el primer esqueleto visible del juego)

> No te fíes de este documento: compruébalo (git, batería). Lo marcado **SIN PROBAR** no se ha visto en un navegador.

## En una frase
Auditada la sesión 5 (tabla abajo). **Aprender en un hilo (spec/11)** hecho y en verde: el servidor ya no se para cuando
una red aprende (entrenos a cualquier velocidad, duelos y exhibiciones). Arreglado el **duelo mix** (absorbía cada
partida dos veces). Y, por decisión del usuario ("quiero que hagas de verdad el juego y que no hagas tests más o menos
innecesarios hasta P11"), empezado **P5**: la portada, el menú con las 3 partidas guardadas y las 4 etapas navegables
encima del laboratorio de siempre.

## Decisiones del usuario de esta sesión (en plan2, fila "Sesión 6")
- Duelo mix: "una vez, el repaso reutiliza" (spec/06 §6.2).
- Exhibiciones: también aprenden en un hilo.
- Quedarse + afinar: se queda como dice la spec (un reajuste pequeño; si es imposible, castigo).
- Carpetas gw-* de TEMP: borradas (1 474) y run-all borra al acabar las que crea la batería.
- La media de la ventaja empieza en 0 y Adam en t = 0 (spec/04 §2).
- **Proceso nuevo (memoria `grafwar-game-first`)**: el juego visible primero; tests solo los necesarios hasta P11 (nada
  de mutantes ni ficheros de "huecos"); mientras se trabaja, solo los tests de lo que se toca; la batería entera (~34
  min) solo al cerrar una fase. Explicar la jerga ("la batería" = los 99 tests seguidos).
- Orden: el usuario quiere "algo funcional" ya → se empezó P5 (esqueleto visible) antes que P3 y P4, que se meten en
  las etapas que los enseñan. **Confírmalo con él al empezar** (lo dijo como "haz de verdad el juego", no eligió la opción
  literal de saltar a P5).

## Auditoría de las sesiones 4 y 5 (hecha y comprobada)
| # | gravedad | dónde | qué | estado |
|---|---|---|---|---|
| 1 | medio (fallo real) | `evo/duel.js` | duelo mix: cada partida absorbida dos veces (recuerdos repetidos, rival con 12 partidas en vez de 6, términos contados dos veces) | arreglado, test `duelo-mix` (congelado en rojo antes) |
| 2 | medio | spec/11, relevo s5 | el "0,85 s por sueño de lote 4" no se reproduce (0,15–0,5 s) | premisas re-medidas; `hilos-c` usa redes de 256 neuronas y lotes de 8 |
| 3 | medio | `hilos-b` | la premisa mide desde la partida 0 y la comprobación desde la 1 | se deja; `hilos-c` cronometra desde el POST |
| 4 | medio (hueco) | `train.js:103` (`mean`) | solo lo caza el test nuevo `huecos-s5` (Adam de libro, EMA desde 0) | cerrado |
| 5 | bajo/diseño | `policy.js` | quedarse + afinar puede castigarse | decisión: se queda |
| 6–12 | bajos/info | varios | exhibición en el hilo principal, x1/x10 sin hilo, `slid` en un comentario, tests que no esperan a que pare un entreno, batería de 23–34 min sin causa aislada, datos sueltos en `evo/` de las 12:30 | resueltos o anotados |
Comprobado también: P1b cumple spec/10 y AGENTS.md; la carrera de api-verdad no se repite en los otros 19 tests con
servidor compartido; los "equivalentes" de s4/s5 lo son; mutantes train.js 103/264/293/575: 13/17 (los 4 que quedan,
equivalentes; ver spec/mutantes.md "Sesión 6").

## Git (compruébalo)
- Commit de tests (huecos-s5, duelo-mix, hilos-c congelados) y commit del código de spec/11 + arreglo mix, con la batería
  completa en verde (bateria-2). Después, commit de P5 (esqueleto). Mira `git log`.
- Congelados: 99 (con huecos-s5, duelo-mix, hilos-c); **client.spec y troops.spec retirados** con P5 (probaban la
  portada vieja con barra de disparo; decisión de la ronda 17 del plan).
- `test/huecos-s4.spec.mjs` (resetTrainerSeq, recorte ±3 del ajuste del movimiento, soldado caído): escrito, **sin
  ejecutar ni congelar, fuera de run-all y sin commit** — con la regla nueva es opcional; si se usa, pasarlo primero.

## P5, lo que hay (sin prueba visual del usuario todavía)
Comprobado sin ventana (Chrome headless por el protocolo de DevTools, sondeo `cdp-probe.mjs` del scratchpad, se pierde):
las 12 rutas cargan sin excepciones ni errores de consola, cada etapa monta su vista, y en la etapa 3 el botón lanza un
duelo x10 que se sigue en directo (candidatos, certeza, registro, marcador). No comprobado: cómo se ve (colores,
tamaños a 1280 y 1920, animaciones), la papelera desde Cargar, Ajustes aplicados.
- `public/index.html` nuevo (sustituye al cliente viejo): portada + juego. `public/evo.html` redirige al juego.
- `public/js/game/routes.js` (rutas; los `#` viejos del laboratorio siguen valiendo), `main.js` (portada, menú,
  Continuar/Nueva partida/Cargar/Academia/Ajustes, 4 etapas con las vistas del laboratorio dentro), `portada.js` (fondo
  animado 2D con curvas y polvo que huye del ratón; constelación de la reina con sus bloques reales y los pulsos de
  activación de su última partida), `espectar.js` (etapa 3: lanzar un duelo x10/x1 y mirarlo con candidatos, decisión y
  registro; también `#room=CODE`), `ajustes.js` (calidad, movimiento, velocidad por defecto; en el navegador).
- `public/css/game.css`: estilo del mockup encima de lab.css.
- Falta de P5: ficha lateral de cada red (Verdad y Quirófano hoy son pestañas de la etapa 4), librerías de animación
  (GSAP, Motion, tsParticles, Lenis), fluido WebGL, parche del DOM sin parpadeo (`patch.js`), menú de Academia (llega con
  el tutorial, P10), nivel del editor sincronizado con el mundo (hoy se pone al crear/abrir el mundo).

## Pendiente, en orden
1. Que el usuario pruebe P5 (lista "en qué fijarte" del final) y arreglar lo que vea.
2. Seguir P5 (lo que falta, arriba) y P6 → P11 con la regla nueva de tests. P3 ("lo que aprende de este tiro") y P4
   ("seguir solo N generaciones") dentro de las etapas 3 y 4.
3. P11: la verificación completa (batería, mutantes del código nuevo de spec/11 y de P5, huecos).

## Problemas de esta sesión
- La batería tarda 23–34 min; el usuario se quejó ("llevamos 3 días"). De ahí la regla nueva.
- `node -e` con comillas en bash rompió una fila de plan2 (se arregló): usar ficheros para los scripts.
- Las medidas en rojo de `hilos-c` se tomaron con una prueba de mutantes corriendo a la vez (dicho en spec/11 §3).

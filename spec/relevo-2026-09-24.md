# Relevo 2026-09-24 — ronda 17 (Entrenador de redes), fin de la sesión 2

> La sesión 1 (a69191d8) dejó P1 escrito pero sin commitear ni batería, y una lista A1–A14 para auditar. La sesión 2
> (esta) auditó todo, pidió los OK y dejó P1 casi cerrado. **No te fíes de este documento: compruébalo** (git, tests,
> mutantes). Lo que sigue es lo que la sesión 2 hizo y midió, con dónde mirar.

## Dónde estamos, en una frase
P1 (terreno de círculos que se rompe y tiro que atraviesa) está **cerrado, commiteado y subido** con la batería en verde
(TODO OK, 24 min 24 s, 80 grupos, sobre 29de45f). Después se congelaron tres tests nuevos (583c4cf); uno,
`receta-extra`, está **en rojo a propósito** hasta arreglar A1 (receta, aprobado por el usuario): hoy la batería completa
sale en rojo solo por él.

## Git (compruébalo con `git status` y `git log --oneline -10`)
- Subido a origin/master, en orden: 0b20bc9 (test P1 de la sesión 1) · 2f753b1 (congelados cambiados con OK: puntos
  1–7 y semilla 5) · f740142 (terreno-extra en rojo + terreno-api) · 9fdbbf8 (dos premisas más rotas por el mapa,
  mismo arreglo que la semilla 5) · **29de45f (código y documentación de P1)** · 583c4cf (terreno-extra-b, ui-sse-b y
  receta-extra congelados) · el commit de este relevo.
- Sin commitear: nada. Sin seguimiento: `referencianoabsoluta.png` (del usuario; no tocar).

## Qué se rompió y cómo quedó
| # | gravedad | qué pasaba | estado |
|---|---|---|---|
| N1 | **grave** | P1 hacía el juego 10–30× más lento: partida sin pantalla ~35 ms → 0,36–1 s (máx. 4,6 s); boletín 2 s → 17 s; entreno turbo de 40 partidas 4,2 s → >61 s, y el servidor dejaba de responder hasta 5,7 s (de ahí los `fetch failed` de api-trainings, api-verdad y arreglos-ocupada). Causa (perfil de CPU): `slideMove` = 70 % del tiempo (rejilla polar de 2 880 puntos × `segmentClear` × hasta 22 círculos con `Math.hypot`). | ✅ dos atajos **exactos** en `shared/geometry.js` (podar por distancia antes de validar; descartar por la caja del círculo antes de `hypot`): partida ~0,2 s, boletín ~5 s, entreno 10,7 s, servidor máx. 0,55 s. Guardas: `terreno-extra` (equivalencia con la rejilla completa y tope de 2 s para 1 000 deslizamientos). spec/01 §10.5b. |
| N2 | medio | `rooms.coverOf` (cobertura de los eventos `move`, recompensa "cubrirse", examen de cobertura) no veía los bocados. | ✅ |
| N3 | medio | `shared/policy.js`: el dibujo de candidatos de una red sin Simulador y el ajuste fino del movimiento no veían los bocados. | ✅ |
| N4 | medio | El Simulador medía `minDist` sobre todo el recorrido (el tiro atraviesa): 0,05 en vez de ~0,7 en un tiro que mata; spec/01 §10.5 prometía "lo mismo que antes". | ✅ hasta el primer impacto (`firstHit`, exacto al solver de antes). |
| P7 | diseño | El plan decía "el Simulador cuenta a cuántos mata"; la sesión 1 hizo "primer impacto + opción Contar bajas". | ✅ usuario: "más RL, como el plan". Las dos primeras entradas = cuántos enemigos / aliados alcanza (0–4); con un impacto = 1 (Vidente vale). Sin opción aparte. Como la IA del original (`ComputerPlayer`: ±2 000 000 por soldado). |
| N5 | bajo | Ojo Compañeros: "mató / fuego amigo" miraba el `type`; un tiro [enemigo + aliado] no contaba como fuego amigo. | ✅ por `kills`/`friendly`. |
| A11 | bajo | La capa dibujada del terreno solo cambiaba de clave con el número de obstáculos y bocados. | ✅ `terrainKey` con todas las coordenadas. |
| — | bajo | Código muerto `rooms.reposition()`, comentarios de "renovar mapa", `agent/remote.mjs` sin `bites`, spec/03 sin tocar, AGENTS.md viejo. | ✅ |
| A1 | medio | Receta: con `duration.minutes`, el examen de antes cuenta como entreno y hace avanzar los programas (con carga, un entreno de 12 s entrenó 3,3 s); las pausas también cuentan. | 🟡 **test congelado y en rojo** (`receta-extra`, 583c4cf); arreglo pendiente (ver "Qué falta" 1). |
| A3 | bajo | Volver a una versión conserva el Adam actual (la spec dice "se reinicia si cambia la estructura", pero solo mira el número de parámetros). | ⏸ el usuario no lo eligió. |
| — | bajo | Parar durante el examen de después: el entreno acaba `done` y con `exam.after`. | ⏸ no elegido. |

Congelados cambiados con OK del usuario (motivos en los commits): terreno.spec (1: x 1,8; 2: fin = −6 − HIT_RADIUS;
3: impactos por posición del soldado; 7: Simulador con cuentas), rooms.spec (4: sin renovación), motor.spec (5: su
oráculo propio entiende círculos y bocados), percepcion.spec (6: Simulador = solver hasta el primer impacto),
entrenador-extra, entrenador-extra-b, trono, arreglos-exhibicion (semilla 5: se busca la primera en la que la red
decide). **Revisar 9fdbbf8**: arreglos-metodo-extra y api-verdad tenían la misma causa y se les aplicó el mismo arreglo
sin preguntar uno a uno (se le dijo al usuario que se haría con los de la misma clase). En arreglos-metodo-extra la
premisa pasó a "14 partidas en que los dos soldados de la red deciden y mueren", para que el lote pase de 20
observaciones de un término; comprobar que eso no debilita lo que prueba.

## Qué se hizo en la sesión 2, en orden
1. Auditoría con ojos nuevos (ejecutando, no leyendo): batería completa con P1 (11 grupos en rojo), mutantes en un
   worktree limpio de HEAD, perfil de CPU (`node --cpu-prof`), sonda de latencia del servidor durante un entreno.
2. Hallazgos enseñados al usuario y OK pedidos: 1–6 "con mis oráculos"; 7 "más RL como el plan, piensa en el
   original"; semilla 5 "buscar la semilla en la que la red decide"; receta "A1 y huecos de mutantes (queda mucho
   trabajo: tutorial, UI y bucle jugable)".
3. Commits de tests 2f753b1 y f740142; código de P1 (N2–N5, P7, A11) con un paso aparte para la optimización exacta y
   otro para el código muerto; 9fdbbf8; documentación (spec/01 §10.3–§10.6, spec/03 §2, AGENTS.md, plan2 ronda 17).
4. Mutantes de P1 (resultados abajo) y tests de sus huecos en `terreno-extra-b` (sin congelar).
5. Tests de la receta: `receta-extra` (A1 en rojo + huecos de recipe.js en verde) y `ui-sse-b` (desuscribirse dos
   veces), sin congelar.
6. **Lo último antes de cerrar**: añadir a `terreno-extra-b` los casos de la sala (bocado contra un rectángulo; roces
   tras un tiro que atraviesa) y recoger los resultados de mutantes. **Después**: batería completa en verde con el código
   de P1 (TODO OK, 24 min 24 s) → commit 29de45f → congelar y añadir a run-all terreno-extra-b, ui-sse-b y receta-extra
   (583c4cf) → este relevo → push. La tanda de mutantes de readBody se paró a medias (2 de 14, ambos cazados).

## Qué falta, en orden
1. **A1** (la batería está en rojo solo por `receta-extra`): en `evo/train.js`, un reloj del entreno que arranca tras el
   examen de antes y descuenta las pausas (el bucle de pausa está al principio del `while`); usarlo en `progress()` y
   en `doneBy()` (minutos); `startedAt`/`elapsedMs` siguen siendo la duración total (spec/04 §9.8). `receta-extra` en
   verde → spec/04 §11.2 → batería en verde → commit → push.
2. **readText / readBody** (`evo/api.js:298-312`, `server/server.js:38-52`): leer spec/08 §10.6 y escribir tests del
   tope exacto (¿exactamente el tope pasa?) y del corte a 4× el tope; readBody quedó sin cruzar entero con los tests
   del 413 (api-lab, arreglos-parte3-extra-api, arreglos-sala, auditoria-p0-api).
3. **Mutantes**: repetir los de P1 con `terreno-extra-b` incluido y escribir la sección P1 de `spec/mutantes.md`
   (cada superviviente, cazado o justificado). Decidir `percept.js:234-237` (respaldo para simulaciones sin cuentas que
   nadie usa: quitarlo como *Dead Code* o justificarlo). Los de `recipe.js`, `sse.js` y nombres, también a mutantes.md.
4. Memoria (`grafwar-juego-ronda17`) y plan2 al día; después, P2 (mundos) según el plan.

## Mutantes de la sesión 2 (herramienta: `node tools/mutants.mjs <fichero> --lines a-b,… --tests t1,t2 [--server --port N]`)
Resultados con los tests de P1 de entonces (sin `terreno-extra-b`, que se escribió después para cazar los huecos):
- `shared/geometry.js` 160/182. Supervivientes: 16 ×8 (el descarte por caja: equivalentes por diseño) · 17 y 26
  (bordes del círculo y del bocado → cubiertos por terreno-extra-b) · 32 y 42 (ya justificados en F1) · 55, 56, 60, 67,
  68 ×2, 70, 74 ×2, 86 (rejilla y `los`: **sin clasificar**).
- `shared/solver.js` (líneas cambiadas) 16/22: 19 (ángulo por defecto) y 57 (`<=` en igualdad exacta) equivalentes ·
  60 ×4 (`firstHit.points` → cubierto por terreno-extra-b).
- `shared/reward.js` (39–47) 24/25: 45 (`*`→`/` con un solo aliado → cubierto por terreno-extra-b).
- `shared/percept.js` (líneas cambiadas) 185/234: 18, 221 ×2, 238, 239 ×2 → cubiertos por terreno-extra-b · 216 ×2 y
  224 equivalentes · 234–237 ×13 = respaldo muerto (decidir) · 256, 261, 262, 265, 267 ×2, 301, 344 ×2, 363–365 ×7,
  406 ×11 **sin clasificar**.
- `server/mapgen.js` 80/209: la mayoría cubiertos por terreno-extra-b (distribuciones, 2–3 grandes, llanura, rangos,
  hueco de reserva). Quedan: la altura exacta de los círculos grandes (diseño), desempates del hueco de reserva, el
  1e-12 y el signo del coseno de Box–Muller (equivalentes).
- `server/rooms.js` (líneas cambiadas) 29/67: 390 ×16 (rama de rectángulos del bocado) y 407 ×3 (roces) → cubiertos
  por terreno-extra-b · 401 ×8 (turno y recorte de la expresión en shotLog) y 224 ×2 (jugadores de game.start):
  contrato de antes en la misma línea, sin test en P1 · 68 equivalente · 324, 418, 434 ×4, 436 ×2 **sin clasificar**.
- `shared/policy.js` 2/2 · `public/js/render.js` 0 mutantes (la herramienta no entra en plantillas; lo prueba
  terreno-extra) · `evo/recipe.js` 246/292 (huecos → receta-extra) · `public/js/ui/sse.js` 17/18 (→ ui-sse-b) ·
  `evo/mutate.js` nombres 100 % · `evo/api.js` readText 6/11 con todos los tests del 413 (299, 303 ×2, 304 ×2).

## Medidas (para comparar)
Partida sin pantalla (red contra Sniper L3, 2 soldados, semillas 1–12): antes de P1 ~33 ms · P1 0,36–1 s · P1
arreglado ~0,2 s (máx. 0,8). Boletín: 1,6–2,1 s · 17–18 s · 4,6–5,4 s. Entreno turbo 40 partidas: 4,2 s · >61 s ·
10,7 s. 1 000 deslizamientos contra 20+ círculos: 5,3 s → 0,3 s. Las partidas tienen ~14 tiros en vez de ~5 (el terreno
tapa más): eso es el juego nuevo, no un fallo. Coste de antes que sigue: `normalize()` clona el genoma entero dos veces
por decisión (17 % del tiempo que queda).

## Pendientes conocidos que nadie ha aprobado (no tocar sin preguntar)
A2/A3 (Adam al volver a una versión o a la mejor) · parar durante el examen de después · la explosión se dibuja al
final del recorrido y no en las víctimas (UI) · el heurístico descarta cualquier tiro con un aliado (el original suma
±) · `normalize()` que clona dos veces por decisión.

## Fases que faltan
P2 mundos · P3 lo que aprende · P4 seguir solo · P5 esqueleto, portada y librerías descargadas · P6 editor · P7 duelo ·
P8 entrenar (+ UI de la receta y de las versiones) · P9 trono y resultados · P10 tutorial y Academia · P11 cierre.
Plan completo: `~/.claude/plans/pasted-content-id-4aad-trabajas-en-cozy-cake.md`.

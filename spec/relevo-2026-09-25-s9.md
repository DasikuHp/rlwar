# Relevo 2026-09-25 — sesión 9 (auditoría de la sesión 8, primera parte)

> **No te fíes de este documento ni del código de las sesiones 8 y 9**: compruébalo ejecutando (git,
> `node test/run-all.mjs --check-only`, tests del cliente y el sondeo sin ventana `tools/sondeo.mjs`). Nada de esto se ha
> visto en un navegador con ventana: lo ve el usuario.

## En una frase
Se auditó por sondeo la mitad de la lista de la sesión 8 y se arregló lo que salió (commit **ceae358**). La sesión se
cerró a ~280k de contexto. P6 sigue sin cerrar; P7 → P11 pendientes.

## Qué pasó, en orden
1. Comprobación: master = origin (5c107c1), 98 congelados con huella OK, cliente 21/21.
2. Preguntas al usuario (plan2, fila "Sesión 9"): **no probó la lista de la s8**, pero miró "el tutorial" (lo que hay:
   la pantalla "Tu red está vacía" de Desde cero y las pistas) y dijo que **"sigue sin ser didáctico y explicativo;
   busca bien cómo se hace un buen tutorial"**. Versiones: **"las de entreno nunca se borran"**.
3. Sondeos (en el scratchpad, no en el repo): alturas de todas las etapas y la ficha a 1280×800 y 1920×1080; Probar ya
   con un duelo en directo a la vez; volver al duelo; arrastrar cables con eventos de ratón reales (CDP
   `Input.dispatchMouseEvent`); qué guarda una partida.
4. Arreglos y commit **ceae358** (detalle abajo). Cliente 21/21; `receta-api`, `api-lab`, `arreglos-almacen`,
   `api-evolucion` en verde (`api-lab` y `api-evolucion` necesitan la URL de un servidor como argumento).

## Auditoría: qué salió
| Punto del relevo s8 | Resultado | Qué se hizo |
|---|---|---|
| Alturas tras `.g-body` flex (todas las etapas, 1280 y 1920) | **Bien**: nada se sale. Lo que medía de más (≤10 px) era la animación de entrada (`enter`, y: 10) a medias | nada |
| Editor a 1280×800 | **Mal**: lienzo de 326 px y Tortuga pide 411: un cuarto de la red escondido bajo el panel; no había dónde soltar un cable | "Encajar" cabe a lo alto también (55–100 %); panel de abajo al 30 % del alto; cabecera baja en ventanas ≤860 px; el lienzo se desplaza solo al arrastrar un cable junto al borde |
| Plantillas del raíl | **Mal**: su porqué se cortaba sin "…" | en otra línea |
| Leyenda de cables en el panel | **Mal**: cortada a 1280 | se quita a <1500 px (cada cable lo dice al pasar el ratón) |
| Cable que pasa por debajo de una tarjeta | **Mal** (visto: c → ch parecía entrar en cd) | los cables largos rodean las tarjetas por el hueco más cercano (`wirePath`) — **sin ver con ventana** |
| Probar ya | **Mal**: (1) con las rivales ocupadas daba un 409 sin decir quién; (2) con un duelo en directo en la etapa 3, **las dos salas pintaban en el mismo plano** (la escucha del duelo seguía viva) | `game/sala.js`: una sola escucha para el duelo y Probar ya; quien toma el plano deja a la otra sin escuchar y, al volver, la retoma sembrando las últimas curvas del estado. Probar ya marca "— en un duelo" / "— entrenando" y elige una libre |
| Volver al duelo tras Probar ya | **Bien** tras el arreglo: sigue la sala en directo, con sus curvas | — |
| Arrastrar cables | **Bien** (marcas verde/gris, soltar donde no vale → aviso con el porqué, soltar donde vale → se une). El primer sondeo falló porque los bloques estaban bajo el panel | — |
| Banco: "una seno", "+-" | **Mal** | "un seno", "una recta"…; `prettyExpr` en render.js ("+-" → "-") en bocadillos, barra, registro, candidatos y banco |
| x10 "ver siempre las líneas" | **A medias**: solo se guardaban 24 curvas de hasta 90 | todas las de la partida (96) |
| VERSIONS_KEEP | decisión del usuario | las de entreno no se borran nunca; el tope de 50 poda solo las del editor y "volver a esta" (spec/04 §11.7). Comprobado con un guion: 65 guardados → 2 de entreno + 50 del editor |

## Lo que queda por auditar (de la lista del relevo s8)
Cambiar el **Tipo** de un bloque · histograma → **Quirófano** de la ficha · **Tu escena** con el ratón · atajos
(Ctrl+Z/Y/S, flechas, Supr) · menú ⋯ (abrir otra red, importar, exportar, borrar) · plegar el panel y la raya ·
bocadillos en un duelo x10 **largo** (en uno corto se ven bien) · reglas viejas de `lab.css` tapadas por `crear.css`.
Visto de paso, para P7: en el panel del duelo "margen −0.03" no se entiende (la elegida no es la más probable porque se
sortea) y "certeza 0.00" sin explicar.

## Lo que falta de P6
1. **Moviola en el mismo sitio** (ver otra vez las 6 partidas dentro de Probar ya). La partida guardada trae
   `game.start` (obstáculos), cada `shot` (modo, función, ángulo, bocado, resultado), cada `move` (from/to) y las
   bajas, **pero no las curvas ni dónde empieza cada soldado**. Diseño propuesto: (a) servidor, `server/rooms.js`: añadir
   a `game.start` `positions: [{id, playerId, team, x, y}]` (campo nuevo, no cambia nada más); (b) módulo puro
   `public/js/game/moviola.js` que rehace cada tiro con `shared/solver.js` (obstáculos + bocados hasta ese tiro +
   posición del tirador) — el motor es determinista, así que sale exacto; (c) un test que compare las curvas rehechas
   con las del SSE de una partida real (headless); (d) en Probar ya, al acabar: las 6 partidas, ▶, pausa, tiro a tiro.
   Partidas viejas sin `positions`: la posición inicial sale del primer `move.from` de cada soldado (si nunca se movió,
   no se puede: se dice).
2. **"+ Añadir capa" sobre un cable** (insertar un bloque entre dos).
3. El nivel del editor sincronizado con el del mundo (`GET/PUT /api/worlds/:n/meta`).
4. **Pistas que se entiendan sin saber nada** — y el aviso del usuario de esta sesión: investigar de verdad cómo se
   hace un buen tutorial (con fuentes: plataforma + URL/autor, regla de CLAUDE.md) antes de rehacer "Tu red está
   vacía" y de diseñar P10; preguntarle con opciones y ejemplos.
5. Lo que queda por auditar (arriba).

## Siguiente, en orden
1. Auditar lo de la s9 sin fiarse (sobre todo los cables que rodean y la cabecera baja) y lo que queda de la lista s8.
2. Investigar tutoriales buenos y preguntar al usuario el diseño; cerrar P6 (moviola, añadir capa, nivel, pistas).
3. P7 (con P3 "lo que aprende de este tiro"; la escucha común ya está en `game/sala.js`) → P8 → P9 (con P4) → P10 →
   P11 (AGENTS.md: `lastShot.angle`, `PUT /nets` con `version`, versiones de entreno sin borrar).

## Ficheros sin seguimiento (no son de esta sesión)
`referencianoabsoluta.png` (la imagen de referencia del mockup) y `test/huecos-s4.spec.mjs` (un fichero de huecos de la
s4; la regla dice que no hay huecos hasta P11): siguen fuera de git; preguntar qué hacer con ellos.

## "En qué fijarte" (sesiones 8 y 9) — `node server/server.js` y `http://localhost:8787/`
1. **Editor a 1280** (o una ventana de portátil): Tortuga se ve **entera** al abrirla ("Encajar"); las plantillas dicen su
   porqué en una segunda línea; la cabecera de la etapa es más baja. Fallo: bloques escondidos bajo el panel.
2. **Cables largos**: el de Candidatos → Elegir rodea las tarjetas del medio; no parece entrar en ninguna. Arrastra un
   cable desde el círculo de la derecha de Reloj: verde donde vale, gris donde no; al soltar en gris, dice por qué.
3. **Probar ya con un duelo en marcha**: lanza un duelo x10 en la etapa 3, vuelve al editor y pulsa Probar ya. Las redes
   del duelo salen "— en un duelo" y no se pueden elegir; la partida de prueba se ve sola en su plano. Ciérrala y vuelve
   al duelo: sigue su partida con sus curvas. Fallo: curvas de las dos partidas mezcladas.
4. **Duelo x10**: todas las curvas de la partida se quedan (las viejas tenues); en la barra y los bocadillos no sale
   "+-". El banco del editor dice "un seno", "una recta".
5. De la s8 (sin probar aún): tinta de la portada en Ajustes; Cargar o borrar; Desde cero; Velocidad Turbo.
